<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.hotelreservationapi.mappers.CardMapper">
    <resultMap id="CardResultMap" type="com.example.hotelreservationapi.domains.Card">
        <id property="id" column="id"/>
        <result property="number" column="number"/>
        <result property="balance" column="balance"/>
        <association property="user" column="user_id" javaType="com.example.hotelreservationapi.domains.User">
            <id property="id" column="uId"/>
            <result property="login" column="login"/>
            <result property="password" column="password"/>
            <result property="name" column="name"/>
            <result property="surname" column="surname"/>
            <result property="middlename" column="middlename"/>
            <result property="email" column="email"/>
        </association>
    </resultMap>

    <!--<resultMap id="UserResultMap" type="com.example.hotelreservationapi.domains.User">
        <result column="id" property="id"/>
        <result column="login" property="login"/>
        <result column="password" property="password"/>
        <result column="name" property="name"/>
        <result column="surname" property="surname"/>
        <result column="middlename" property="middlename"/>
        <result column="email" property="email"/>
    </resultMap>-->

    <select id="readUserCards" resultMap="CardResultMap">
        select cards.id, cards.number, cards.balance, users.id as uId, users.login, users.password, users.name, users.surname, users.middlename,users.email
        from cards left JOIN users on cards.user_id = users.id
        where cards.user_id = #{userId}
    </select>

    <insert id="create">
        insert into cards(user_id, number)
        values (#{card.user.id}, #{card.number});
    </insert>

    <delete id="delete">
        delete
        from cards
        where id = #{id}
    </delete>

    <update id="updateBalance">
        UPDATE cards
        SET balance = #{balance}
        WHERE id = #{id};
    </update>

    <select id="readById" resultMap="CardResultMap">
        select cards.id, cards.number, cards.balance, users.id as uId, users.login, users.password, users.name, users.surname, users.middlename,users.email
        from cards left JOIN users on cards.user_id = users.id
        where cards.id = #{id}
    </select>

</mapper>